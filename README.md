Login и password: admin.

Введите 'admin' чтобы увидеть посты

При перезагрузке страницы данные не теряются, а сохраняются с помощью 'redux-persist'.

Запрос данных происходит с помощью axios.

В данном проекте использовал 'redux-toolkit'.

Возможно слишком много Style компонентов, но если честно, то использовал первый раз библиотеку Styled Components.

Спасибо за внимание.
